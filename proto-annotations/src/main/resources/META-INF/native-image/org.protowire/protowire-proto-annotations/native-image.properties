# GraalVM native-image hints for protowire-proto-annotations.
#
# Pure generated protobuf code (the (pxf.required), (pxf.default),
# (sbe.template_id) etc. extension descriptors). No reflection.

Args = --initialize-at-build-time=org.protowire.proto
